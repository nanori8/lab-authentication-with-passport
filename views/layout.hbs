<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel='stylesheet' href='/styles/style.css' />
  </head>
  <body>

    <nav>
      <a href="/">Home</a>
      {{#if user}}
        {{!-- <a href="/authentication/private">{{user.username}}'s Profile</a> --}}
        <form action="/authentication/sign-out" method="POST">
        <a href="/authentication/private"><img src="{{ user.photo }}" class="profile-picture">{{  user.username }}'s Profile</a>
          <button>Sign Out</button>
        </form>
        {{else}}
        <a href="/authentication/github">Sign In with GitHub</a>
        <a href="/authentication/sign-in">Sign In</a>
        <a href="/authentication/sign-up">Sign Up</a>
      {{/if}}
    </nav>
    {{{body}}}
  </body>
</html>
